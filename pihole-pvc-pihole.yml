apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-pvc-pihole
  namespace: pihole
spec:
  storageClassName: openebs-jiva-csi-default
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
