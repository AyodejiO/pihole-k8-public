apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-pvc-dnsmasq
  namespace: pihole
spec:
  storageClassName: openebs-jiva-csi-default
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
